Handling Files in SF
1.Content Document - Holds info about file , word document , ppt etc

2.Content Document will have one or more ->Content Version(content of the doument that too version wise , IsLatest Field is there to identify the 
                                          latest version , version Data gives the info od content of the content document
 Content Document(parent) is connected to Content Verrsion (child) through content document ID
These 2 object can sit indeopendently , we can cerate diffrent vesions
when we upload a file in files in SF it creates Content Doc and Content version record

3.2.Content Document Link (Junction Object)- Now if we connect that file to an object Acc , contact etc. we do that through Content Documet link
                                             Connects S-object and File

//When a document is created in files content document link is created linking the doc to user , it can be seen in shared with section
//Content Document and version has cascade lookup relation i.e parent deleted so child also deleted and then Link entry also gets deleted

//when we upload a file from notes and attach section of object then 2 entries are created in the Content Document Link
  1 for user , 1 for Object

//first used id in content document version tells which object the file was first associated to


//TEST CLASS

@isTest annotation is used over class name as well as method name
//if there is no test method in class it is not considered as test class , there will be no code coverage
//@TestSetup annotation is used over the method that creates data that can be used across different test methods.
//if Test Setup method is failing then other test method will not run
//if test setup provided data is changed in one of the test method then for next method orignal test setup created data is available (chnages in one method are reverted)
//create atlist 1 test method for every method
//Test.Start and Test.stop should be used only for the code that is triggering the logic
//if test setup is not creating the data for your case then create data in the test method
////Utility Class->Setup Method->TestCases(Test Methods)
//Create sepetrate utility class for diffrent object
//Assert methods
//There is a private method in a class that is used inside the class commonly , this method
//being private willnot be available in test class , to test this method via test class
//we user @TestVisible annotation above method
//RUN AS : Running the test as a real user not as admin

SOME TOOLS
PMD , SMD , SONAR CLOUD , SONAR CUBE etc.
